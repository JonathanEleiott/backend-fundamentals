{
  "name": "servers",
  "version": "1.0.0",
  "description": "- takes in a request from the frontend - OPTIONAL: can make a request to a DB if necessary - responds to the frontend",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "nodemon": "^3.1.0"
  }
}
